# RL Editor

This repository now contains a modular Python GUI architecture for RL task design,
training monitoring, episode inspection, and derived-task experimentation.

## Run

Run the app:

```bash
uv run rleditor --env frozen_lake
```

